Pin the Docker base image to `ghcr.io/openmethane/cmaq:1.0.2` (Debian trixie) instead of the floating `stable` tag, and add `-fbacktrace` to the forward build's link flags to match the backward build. Note that the trixie base compiles with gfortran 14 rather than 12, so numerical output may differ slightly from previous builds.
