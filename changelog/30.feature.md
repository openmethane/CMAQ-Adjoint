`ADJOINT_FWD` now measures what vertical advection moves across each layer interface, upward and downward separately, for methane and for air. The domain integral is logged every synchronisation step; setting `CTM_VADV_TOPFLX` additionally writes a gridded file of the model top interface at each output step. See `docs/vadv-top-flux.md`.
