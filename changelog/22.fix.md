Stopped the `CREATE_CHK` environment variable, which suppresses checkpoint creation for finite-difference testing in the forward run, from also suppressing the backward run's opening of those checkpoint files for reading.
